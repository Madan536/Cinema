server:
  port: 2023

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cinema
    username: root
    password: root123*

 #Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
